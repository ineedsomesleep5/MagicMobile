# Deck Studio build 5000000000 — release preparation

Worktree: `MagicMobile-runtime-hardening`, branch `codex/deck-studio-2`.
This receipt supersedes the catalogue/release assumptions in the September 17 Claude handoff.
Upload and tester availability are pending; preparation is not a release.

## Reviewed improvements

- Commander color identity comes from pinned XMage, including both faces, not a front-face text heuristic.
- Curated role data is a hash-pinned, versioned snapshot reproducible in a clean checkout.
- Conservative role fallback excludes quoted, conditional and modal effects. Accuracy checks reject
  missing fixture cards and unintended roles, not just missed positive tags.
- Collection controls scroll on short landscape screens. Collection and deck thumbnails stay visible;
  keyboard submission dismisses the keyboard, quantity changes have accurate feedback, and empty filters
  offer a reset. Artwork preferences explain their app-wide privacy scope in an expandable disclosure;
  the landscape collection toolbar stays compact. The entire search result label opens card inspection.
- Recovery failures explain why closing cannot preserve a draft. Online reference search remains accessible
  when the local catalogue fails to load.
- Phone landscape uses the navigation bar for workspace switching and card count, with no separate deck
  header/tab strip. Secondary controls live in labelled menus; section headers scroll away. A populated
  seven-card UI regression checks usable list height, visible cards and scrolling to the bottom.
- Shared card-rules rendering uses inline mana/tap/untap icons with offline fallbacks and spoken
  accessibility labels. Unknown tokens remain literal; hidden-text normalization is unchanged.
- Catalogue search ranks exact names, name prefixes and other name matches ahead of rules-text mentions
  before result limits and after commander-identity bucket merging. This fixes common lands disappearing
  behind cards that mention them. All filters still apply.
- TestFlight distribution validates the exact processed build, both audiences and Beta App Review state.
  Retries preserve existing review submissions. Apple approval is separate from submission.
- App icon preserves the gold M and five colored discs, removes the inset square border, and uses
  one continuous opaque charcoal background. All nine required icon sizes are exported from the
  edited master; iOS applies its own outer mask.
- Native AI setup exposes XMage's actual skill levels 1–10, remembers the selected level, and defaults
  to desktop level 2. Every AI seat receives the selected level; human seats never do. Higher levels
  allow more upstream thinking time, without changing rules or exposing extra hidden information.
- Ability details have one heading and frame, not a nested duplicate panel. Card-backed choices
  select on tap and inspect on hold; UIKit failure ordering prevents a recognized hold from selecting.
  Rich ability choices are not repeated underneath as compact text actions. Cancel and genuine
  subsequent decisions remain available, with engine IDs/revisions unchanged.
- Compact battlefield cards reserve a footer only for visible power/toughness or tapped/summoning
  status. Otherwise artwork occupies the space; loyalty, defense and other counter overlays remain.

### App icon edit provenance

Built-in image editing, using the previous `AppIcon-1024.png` as the edit target. Prompt:
"Preserve the recognizable gold textured M with red/black outline and the five glossy discs
(white above, green upper left, blue upper right, red lower left, black lower right). Fix only the
inset square/background seam: extend the inner charcoal background to all four edges, removing
the outer matte border. Keep the logo centered and fully visible. Opaque full-bleed square;
no rounded tile, frame, shadow around a tile, transparent edges, or added text. iOS applies the mask."
The generated edit is packaged in `apps/ios/MagicMobile/Assets.xcassets/AppIcon.appiconset/`.

## Local checks

- Deck Studio portable core/finishing/role suites passed, including 104 fallback assertions and
  78 accuracy fixtures (72 required positive role matches).
- App portable Swift suite after symbol and search changes: 303 tests, one skip, no failures.
- Catalogue exporter: 11 self-tests and byte-identical regeneration passed.
- Distribution helper: 15 offline mocked regression tests passed, including the current live CLI response
  shape (which omits empty failures). No release actions during tests.
- iOS simulator build-for-testing passed. Editor/model and bundled catalogue XCTest suites:
  18 tests, no failures. The UI walkthrough passes search, add, inspect, portrait/landscape and discard.
  Simulator screenshots and result bundles are in `build_output/deck-studio-5000000000-*`.
  Final populated seven-card walkthrough passed in 70.829 seconds; visual inspection confirmed inline
  tap/colorless icons and the compact landscape toolbar. Its result is
  `Test-MagicMobile-2026.09.17_10-40-44--0500.xcresult`; five screenshots are retained in
  `build_output/deck-studio-5000000000-populated/`.
  Final native release checks remain pending.
- Shared rules/log XCTest suite after symbol changes: 30 tests, no failures.
- AI skill addition: 7 focused portable setup tests passed (persistence, corrupt values, bounds,
  exact numeric serialization for every level and 1–3 AI opponents). Core: 419 assertions plus
  88 failure-boundary assertions passed. Full real-XMage JVM suite passed; the final wiring
  assertion was then recompiled and rerun through the one-human AI lifecycle successfully.
  These verify upstream settings and JVM behavior, not physical iPhone timing or playing strength.
- The corrected icon passed Apple's asset compiler and all nine size/opacity checks.
- AI setup UI regression passed in 40.251 seconds on the visible iPhone 17 Pro iOS 26.5 simulator:
  default 2, increment to 3, terminate/relaunch retaining 3, decrement to 2.
  Result: `Test-MagicMobile-2026.09.17_11-40-02--0500.xcresult`.
  Direct simulator interaction independently confirmed the same persistence. Earlier headless
  runs intermittently failed at the menu tap; no app-level cause was established. Stepper queries
  were corrected to use the actual combined accessibility label and generated button identifiers.
- Hosted prerequisite run `35246038624` passed for engine source
  `038b103afc9c2c951acb99519d9d6545ee799895`. Native run `35247645533` failed
  before the full ARM64 build: the catalogue probe compared app-only `colorIdentity`
  enrichment against upstream `CardInfo`, which has no such field. The repair validates
  that enrichment separately and still compares every original field exactly. It does
  not change the catalogue bytes or production engine behavior. The same failure was
  reproduced locally, then the corrected JVM and bounded host-native catalogue probes
  passed. Failure details now also print in the workflow log.
  Replacement prerequisite run `35250928638` passed for source
  `f2ffad10e26a25885902e963a13810d8786ed9bc`. Full native run `35252890114` is underway.
- Popup/footer final regression: 95 focused unit cases repeated twice (190 executions), plus
  portrait/landscape swipe, hold, inspect, close/reopen and exact-choice tap flows repeated twice
  (four UI executions), all passed. Result: `Test-MagicMobile-2026.09.17_12-28-37--0500.xcresult`.
  Earlier half-second synthesized holds intermittently arrived as roughly 0.25 seconds. Final
  tests use a sustained one-second hold; the production threshold remains 0.35 seconds. This is
  simulator evidence, not physical-phone timing acceptance. Temporary instrumentation was removed.

## Release gates still required

Fresh same-source hosted prerequisites, full ARM64 native candidate, verified provenance and paired staging,
signed Release archive/export checks, Apple processing, Internal + External group verification and review state.
Physical iPhone gameplay and network-provider acceptance are not established by simulator or fixture tests.

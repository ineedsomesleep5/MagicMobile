{
  "name": "magicmobile-web-play-spike",
  "version": "0.0.0",
  "private": true,
  "description": "SPIKE ONLY: real XMage engine in the browser via CheerpJ, with a headless benchmark. Ships nothing to players.",
  "type": "module",
  "scripts": {
    "build": "tsc --noEmit && vite build",
    "serve": "node scripts/serve.mjs",
    "bench": "node scripts/bench.mjs",
    "jvm-baseline": "node scripts/jvm-baseline.mjs"
  },
  "devDependencies": {
    "playwright-core": "^1.63.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.0"
  }
}
